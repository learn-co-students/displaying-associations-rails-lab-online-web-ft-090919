<%= form_for @song  do |f| %>  #for every song do somthing 
      <%= f.label :title %> #have a label for title 
      <%= f.text_field :title %> #have a text field for title 
      <br />
    
      <%= f.submit %> #submit button 
    <% end %>

