<h1><%= @artist.name %></h1>
<ul>
<%= @artist.songs.each do |song| %> #for every song do somthing 
    <%= link_to song.title, song_path(song) %> #in this case link to song title 
    <% end %>
</ul>

