<%= form_for @artist  do |f| %> # for artist do somthing 
  <%= f.label :name %> #label for name 
  <%= f.text_field :name %> #text field for name 
  <br />
  <%= f.submit %> #submit button 
<% end %>